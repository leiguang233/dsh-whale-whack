# 更新日志 · dsh-whale-whack

> ⚠️ **本插件版已停止更新（2026-09-21）**，这是最后一个版本。
> 后续更新见桌面版：https://github.com/leiguang233/whale-whack-desktop

> 版本号跟随后面的说明：
> 插件版是「DSH 里的一个页面组件」，独立桌面版（锤锤小肥鱼桌面版）**单独编号**，
> 两者共享游戏本体 `lib/client.js`，但交付形态不同，故版本不联动。

---

## 0.2.2（2026-09-21）· 最终版

### 新增

- **锤击音效系统**：锤中时叠加播放三层音（锤击声 + 小黄鸭 + 曼波）
  - 小黄鸭音效取自 `dsh-whale-widget` 的同款素材（`Ya1/Ya2`）
  - 锤击声来自 Mixkit 免费音效库（0.86s / 0.95s 两个短促版）
  - 「曼波」念白（2025 热门梗）用曼波专用 TTS 合成
- **音效自由选择 + 自由导入**（设置页「锤击音效」区）
  - 三条音轨各自下拉：内置预设 / 两者随机 / 关闭
  - 「选择音频文件…」可导入自己的音效（存 localStorage，跨会话保留）
  - 已导入列表带试听 / 删除，单文件上限 4MB

### 公告

- **停止更新**。独立桌面版功能更完整（常驻鲸鱼娘、多屏、纯净桌面、音效导入等），
  且无需安装 DSH。桌面版仓库：https://github.com/leiguang233/whale-whack-desktop

---

## 0.2.1（2026-09-14）

### 视觉

- **头顶气泡改版**：纯白底 + 深灰字 → **深海蓝玻璃渐变 + 金边 + 暖白文字**
  - 与 DSH 皮肤 `maid-atelier` 的夜间态配色统一
  - 宽度 `max-width` 150px → **186px**（长台词从 3 行降到 2 行），并加 `min-width: 54px`
  - 底部尖角改为与气泡同色 + 金边（原来是白三角，改深色底后会突兀）

### 功能

- **冒头速度单位改为「每 6 秒」**（原「每分钟」）
  - 范围 **1~36**（原 1~66），即每 6 秒冒 1~36 只
  - 间隔公式 `60000 / rate` → **`6000 / rate`**
  - 最快 **167ms**（原 909ms），最慢 6000ms
  - 换算封装为 `spawnIntervalMs()`，含 `≥60ms` 防呆下限
- **同屏上限固定为 6 只**，移除设置面板里的「同屏上限」滑块

### 说明

- 本次改动与「锤锤小肥鱼 · 独立桌面版 v1.0.0」**同步**，
  两边 `lib/client.js` 内容逐字节一致。
- ⚠ 桌面版特有的能力（托盘、设置窗、常驻鲸鱼娘、纯净桌面模式、右键菜单主题）
  **不在本插件内**，属于独立桌面版独有。

---

## 0.2.0

- 完全自包含：不再依赖 `dsh-dafeiyu` 提供散发静态图
- 黑底视频动画 13 条 + canvas 前端抠黑
- 真实锤子跟手 + 斜挥砸落动画
- 战绩彩蛋（localStorage 持久化）
